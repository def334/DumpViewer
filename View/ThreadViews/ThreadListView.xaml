<UserControl x:Class="DumpViewer.View.ThreadViews.ThreadListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:DumpViewer.View.ThreadViews"
             d:DataContext="{d:DesignInstance Type=view:ThreadListViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="Border">
            <Setter Property="Background" Value="#FFFFFFFF" />
            <Setter Property="BorderBrush" Value="#FFE5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
        </Style>

        <Style x:Key="CardItemBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF9FAFB" />
            <Setter Property="BorderBrush" Value="#FFE5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF6B7280" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>

        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF111827" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20,18,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Thread List"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="DarkGray" />

        <Border Grid.Row="2" Padding="16">
            <ListBox ItemsSource="{Binding Threads}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardItemBorderStyle}">
                            <Expander>
                                <Expander.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Thread ID: "
                                                       FontWeight="SemiBold"
                                                       Foreground="#FF111827" />
                                            <TextBlock Text="{Binding ThreadId}"
                                                       FontWeight="SemiBold"
                                                       Foreground="#FF111827" />
                                        </StackPanel>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Priority, StringFormat=Priority: {0}}"
                                                   Foreground="#FF6B7280"
                                                   FontSize="11"
                                                   Margin="12,0,0,0" />
                                    </Grid>
                                </Expander.Header>

                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Suspend Count" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding SuspendCount}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="Priority Class" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding PriorityClass}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="TEB" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding Teb}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="Stack Start" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding StackStart}" Style="{StaticResource ValueStyle}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Stack Data Size" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding StackDataSize}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="Stack RVA" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding StackRva}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="Context Data Size" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding ContextDataSize}" Style="{StaticResource ValueStyle}" />

                                        <TextBlock Text="Context RVA" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding ContextRva}" Style="{StaticResource ValueStyle}" />
                                    </StackPanel>
                                </Grid>
                            </Expander>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
